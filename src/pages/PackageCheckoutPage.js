import { PlaceholderPage } from "./PageScaffold.js";

export function PackageCheckoutPage() {
  return PlaceholderPage({ title: "Ödeme", subtitle: "Paket özeti ve ödeme adımı.", route: "/package-checkout" });
}
